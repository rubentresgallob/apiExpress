<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/header') %>
  <main>
    <h1><%= title %></h1>
    <form action="/habits" method="POST">
      <label for="name">Nombre:</label>
      <input type="text" id="name" name="name" required>
      <label for="frequency">Frecuencia:</label>
      <input type="text" id="frequency" name="frequency" required>
      <label for="goalPerPeriod">Meta por período:</label>
      <input type="number" id="goalPerPeriod" name="goalPerPeriod" required>
      <label for="unit">Unidad:</label>
      <input type="text" id="unit" name="unit" required>
      <button type="submit">Añadir Hábito</button>
    </form>
    <ul>
      <% habits.forEach(habit => { %>
        <li><%= habit.name %>: <%= habit.frequency %> (<%= habit.goalPerPeriod %> <%= habit.unit %>)</li>
      <% }) %>
    </ul>
  </main>
  <%- include('../partials/footer') %>
</body>
</html>